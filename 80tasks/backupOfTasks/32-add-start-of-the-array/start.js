/** ЗАДАЧА 32 - Добавление элементов в начало массива
 *
 * 1. Добавьте элементы в начало массива: (решить задачу всего в одну строку)
 *  - сначала строку "Привет"
 *  - потом число 100
 *
 * 2. Выведите длину результирующего массива в консоль
 */
import assert from "assert";

const myArray = [true, null]

// Напишите код здесь

assert.deepEqual(myArray, [100, "Hello", true, null] )
